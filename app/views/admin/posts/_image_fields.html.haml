%li.image{:class => f.object.orientation}
  = image_tag(f.object.oriented_photo_url(:admin))
  = f.input :photo, :as => :file, :label => false
  = f.hidden_field(:_destroy)
  = link_to_function("Remove", "remove_fields(this)")